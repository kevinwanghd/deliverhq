# 项目总览

> 本文件 < 5KB，是 AI 入场的"大堂导览"。
> 修改说明：只改 `desc` 和 `module_id`，其他内容由 Agent 维护时自动跟随。

## 模块清单

| module_id | 模块名 | root_dirs | desc |
|-----------|--------|-----------|------|
| *（示例）* | *（示例）* | *（示例）* | *（示例）* |

### 模块职责说明

> 各模块的核心职责，一句话说明。

### 技术栈

- 语言/框架：
- 构建工具：
- 测试框架：
- 部署方式：

### 项目约定

- 编码规范：
- 分支策略：
- 发布节奏：

---

*本文件由 `check_project_wiki_stale.py` 维护，module_id 变化时自动同步索引。*
