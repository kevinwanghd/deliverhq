# L3：语义桥——抹平"设计/协议"和"代码"的鸿沟

产品语言 → 代码指令的翻译层，是最容易被低估却最能体现工程价值的部分。

## 文件

```
L3-语义桥/
├── README.md              # 本文件
├── glossary.md           # 业务术语 → 代码命名的映射表
├── naming-conventions.md # 代码命名规范（样式/颜色/组件）
└── search-matrix.md      # 5维搜索矩阵（定位用）
```

## 核心原则

> **LLM 写代码前必须先查 L3 映射表，禁止硬编码。**

## 三类语义桥

### 1. 业务术语桥（glossary.md）
"产品/运营说什么" → "代码里叫什么"

### 2. 命名规范桥（naming-conventions.md）
设计稿 token → 代码实现

### 3. 搜索矩阵桥（search-matrix.md）
用户描述 → 代码关键词的5维搜索路径

## Red Line

> ⛔ **RL-L3-01**：UI 改动必须比对 L3 映射表，禁止硬编码字号/颜色/命名。
