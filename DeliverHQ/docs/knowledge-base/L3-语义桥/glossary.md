# 业务术语 → 代码命名映射表

> 产品/运营说的"业务语言" → 代码里的实际命名。
> 维护规则：每发现一个新产品词/代码名的映射，立即补充。

## 映射表

| # | 产品/业务语言 | 代码里的实际命名 | 所在文件 | 模块 | 备注 |
|---|-------------|----------------|---------|------|------|
| 1 | | | | | 示例占位 |

## 移动端专项映射

> 以下映射用于移动端（iOS/Android）项目的需求语义翻译。

### 平台/端关键词

| 关键词 | 含义 | 映射到 |
|--------|------|--------|
| 手机上、手机端、移动端 | 客户端 | `mobile` / `client` |
| iOS、Android | 具体平台 | `ios` / `android` |
| 客户端、App | 移动应用 | `App` / `Client` |
| 小红条、toast、弹窗 | 原生提示控件 | `TipsView` / `Toast` / `Alert` |
| 红点、角标 | 未读标记 | `BadgeView` / `UnreadBadge` |
| 下拉刷新 | 交互手势 | `pullToRefresh` |

### 拦截点映射

| 产品描述 | 代码里的实际位置 | 验证方式 |
|---------|----------------|---------|
| | | |

---

*本表由需求开发过程中持续补充。每次发现产品词→代码名的映射就补充一行。*
