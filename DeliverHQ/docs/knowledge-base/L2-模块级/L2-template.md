---
module_id: {{module_id}}
module_name: {{模块名}}
root_dirs:
  - src/{{module_id}}/
  - tests/{{module_id}}/
desc: {{一句话描述模块职责}}
last_reviewed: {{YYYY-MM-DD}}
sha_baseline: "{{sha256前8位}}"
---

# {{模块名}}（module_id: {{module_id}}）

{{一句话描述模块职责和边界。}}

## 模块边界

- **对内依赖**：{{列出依赖的其他模块}}
- **对外暴露**：{{列出暴露给其他模块的接口}}

## 文件登记表

### Controller

| 文件 | 职责 | 关键方法 | 调用方 |
|------|------|---------|-------|
| | | | |

### Service

| 文件 | 职责 | 关键方法 | 调用方 |
|------|------|---------|-------|
| | | | |

### Model / Entity

| 文件 | 职责 | 关键方法 | 调用方 |
|------|------|---------|-------|
| | | | |

### Repository / DAO

| 文件 | 职责 | 关键方法 | 调用方 |
|------|------|---------|-------|
| | | | |

### 其他（Middleware / Events / Jobs）

| 文件 | 职责 | 触发时机 |
|------|------|---------|
| | | |

## 关键调用链

```
{{模块入口方法}}
  └── {{调用下游模块/服务}}
      └── {{更下游}}
```

## 不变式（不能动的命名和逻辑）

- {{命名约束1}}
- {{命名约束2}}

## 最近变更

| 日期 | 变更 | 关联 CR |
|------|------|---------|
| YYYY-MM-DD | {{变更说明}} | CR-XXX |

---

*本文件由 `check_project_wiki_stale.py` 维护。SHA 基线缓存：{{sha256前8位}}*
