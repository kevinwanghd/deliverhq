# COMMANDS.yml
# Authoritative project commands. Keep this aligned with change-requests/*/verification-manifest.yml.

build:
  enabled: false
  command: null
  working_dir: "."
  timeout: 300

test:
  enabled: false
  command: null
  working_dir: "."
  timeout: 600

lint:
  enabled: false
  command: null
  working_dir: "."
  timeout: 300

typecheck:
  enabled: false
  command: null
  working_dir: "."
  timeout: 300
